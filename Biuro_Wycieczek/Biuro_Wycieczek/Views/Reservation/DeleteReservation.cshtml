@model Biuro_Wycieczek.Models.Reservation
@{
    ViewBag.Title = "DeleteRezerwacja";
}

<h3>Are you sure you want to delete this?</h3>
<div>
    @using (Html.BeginForm("Delete", "Rezerwacja", FormMethod.Post))
    {
        @Html.HiddenFor(r => r.ReservationId)
        <h4>Delete Rezerwacja</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.ReservationId)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ReservationId)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ReservationDate)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ReservationDate)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.IdTrips)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.IdTrips)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.IdClients)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.IdClients)
            </dd>
        </dl>
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to Rezerwacja List", "Index")
        </div>
    }
</div>
